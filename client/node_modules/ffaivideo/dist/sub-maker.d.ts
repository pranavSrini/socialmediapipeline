import { ISubMaker } from './config/config';
declare class SubMaker implements ISubMaker {
    offset: [number, number][];
    subs: string[];
    constructor();
    createSub(timestamp: [number, number], text: string): void;
    generateSubs(wordsInCue?: number): string;
}
declare const fileToSubtitles: (filename: string) => Promise<[number, string, string][]>;
export { SubMaker, fileToSubtitles };
