import { SubMaker } from './sub-maker';
declare const generateSubtitle: ({ subMaker, videoScript, subtitleFile, subtitleMaxWidth, lineBreakForce, isChinese, }: {
    subMaker: SubMaker;
    videoScript: string;
    subtitleFile: string;
    subtitleMaxWidth: number;
    lineBreakForce: boolean;
    isChinese: boolean;
}) => Promise<void>;
export { generateSubtitle };
