import { SubMaker } from '../sub-maker';
import { AzureTTSSettings } from '../config/config';
declare const azureTTS: (text: string, voiceName: string, voiceFile: string, settings: AzureTTSSettings) => Promise<SubMaker>;
export { azureTTS, AzureTTSSettings };
