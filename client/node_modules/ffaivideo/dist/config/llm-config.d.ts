interface LLMConfig {
    apiKey: string;
    modelName: string;
    baseUrl?: string;
    apiVersion?: string;
    responsePath?: string;
    data?: any;
    provider?: string;
    requestConfig?: any;
}
declare const defaultLLMConfig: {
    moonshot: LLMConfig;
    openai: LLMConfig;
    azure: LLMConfig;
    gemini: LLMConfig;
    g4f: LLMConfig;
    gpt4js: LLMConfig;
    customAI: LLMConfig;
    defalut: LLMConfig;
    [key: string]: LLMConfig;
};
export { LLMConfig, defaultLLMConfig };
